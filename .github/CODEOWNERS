# Everything outside aur/ needs owner review when GitHub's Code Owner review
# requirement is enabled. CODEOWNERS alone does not enforce that requirement.
# Last matching entry wins; an empty owner list clears ownership.
#
# Outsiders can submit aur/-only PRs, but cannot start the scheduled/manual-main
# AUR workflow or obtain its credentials. It binds bot author, same repository,
# exact head SHA, labels and publisher status before review and before merging.
# The status must be required from GitHub Actions with no workflow bypass.
#
# Candidate patches are published without checkout or execution. Mechanical
# failures create needs-review drafts with a failing eligibility status. Claude
# sees only the validated diff, has no tools/GitHub credential, and returns only
# a verdict. A separate job owns the fixed-SHA merge or escaped failure comment.
#
# This exception permits unattended routine bumps when configured with matching
# review rules. It does not make arbitrary changes under aur/ safe. Human-reviewed
# drafts need a separate authorized merge path. Keep human bypass credentials
# separate from cloud agents, and protect workflow/script/policy changes.

*       @gablank

/aur/
