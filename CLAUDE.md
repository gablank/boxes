# CLAUDE.md

The agent brief for this repository is shared across all agents. To avoid
maintaining two copies, this file imports it rather than duplicating it. Do not
add repository guidance here — edit `AGENTS.md` (and the `.agents/` skills and
rules it points to).

@AGENTS.md
